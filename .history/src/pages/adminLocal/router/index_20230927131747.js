import LocaPage from '../pageLocal';
import Upload from '../upLoad';

const AdminRoute = [
    {path: '/upLoad',component:Upload},
    {path: '/pageLocal',component:LocaPage}
]

export default AdminRoute